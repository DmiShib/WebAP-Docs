import{_ as t,o as i,c as o,a2 as a}from"./chunks/framework.DnrXFDHb.js";const p=JSON.parse('{"title":"Browser Timings","description":"","frontmatter":{},"headers":[],"relativePath":"configuration/browser-timings.md","filePath":"configuration/browser-timings.md"}'),r={name:"configuration/browser-timings.md"};function n(s,e,c,l,d,m){return i(),o("div",null,[...e[0]||(e[0]=[a('<h1 id="browser-timings" tabindex="-1"><i class="ph ph-clock-countdown"></i> Browser Timings <a class="header-anchor" href="#browser-timings" aria-label="Permalink to &quot;&lt;i class=&quot;ph ph-clock-countdown&quot;&gt;&lt;/i&gt; Browser Timings&quot;">​</a></h1><p>This page details the technical constraints of the WebGL architecture related to the <code>Application.targetFrameRate</code> parameter.</p><p>When compiling a Unity project for WebGL, the <code>Application.targetFrameRate</code> parameter modifies not just the engine&#39;s internal frame limit, but the <strong>baseline browser API</strong> through which the Main Loop operates.</p><h2 id="uncapped-requestanimationframe" tabindex="-1">Uncapped (requestAnimationFrame) <a class="header-anchor" href="#uncapped-requestanimationframe" aria-label="Permalink to &quot;Uncapped (requestAnimationFrame)&quot;">​</a></h2><p>If you uncap the frame rate (by setting <code>Application.targetFrameRate</code> to <code>-1</code> or <code>0</code>, depending on the Unity version), the Emscripten compiler binds the Unity main loop directly to the browser API <code>window.requestAnimationFrame</code>.</p><ul><li><strong>Pros:</strong> This is the native, smoothest, and most accurate rendering method on the web. The browser automatically synchronizes calls with the player&#39;s monitor refresh rate (VSync).</li><li><strong>Profiling:</strong> The <code>cpuMainThreadFrameTime</code> metric (read by WebAP) reflects the <strong>real</strong> time the CPU spends calculating physics and scripts.</li></ul><h2 id="artificial-limit-settimeout" tabindex="-1">Artificial Limit (setTimeout) <a class="header-anchor" href="#artificial-limit-settimeout" aria-label="Permalink to &quot;Artificial Limit (setTimeout)&quot;">​</a></h2><p>If you strictly enforce a frame rate (e.g., <code>Application.targetFrameRate = 30</code>), Emscripten is forced to abandon <code>requestAnimationFrame</code>. Instead, it utilizes the <code>setTimeout()</code> API or a complex combination of internal timers to artificially delay the loop.</p><div class="warning custom-block"><p class="custom-block-title">CPU Metric Distortion (The Emscripten Trap)</p><p>An artificial timer introduces a severe discrepancy when measuring CPU time. The processor may complete all game logic calculations in 5 ms and enter an idle state, but the <code>WaitForEndOfFrame</code> coroutine will not resume until the <code>setTimeout</code> timer expires.</p><p><strong>Consequently:</strong> The <code>FrameTimingManager</code> records this wait time into the CPU timing. The profiler (and WebAP Tracker) will deduce that you have a <em>CPU Bottleneck</em>, even though the processor is functionally idle and the frame is merely awaiting the browser timer.</p></div><h2 id="correct-configuration-strategy" tabindex="-1">Correct Configuration Strategy <a class="header-anchor" href="#correct-configuration-strategy" aria-label="Permalink to &quot;Correct Configuration Strategy&quot;">​</a></h2><p>For optimal Web Adaptive Performance operation in the browser:</p><ol><li><strong>Do not utilize <code>Application.targetFrameRate</code></strong>. Leave it at <code>-1</code> (uncapped).</li><li>Specify the desired FPS within the plugin settings (<code>Target Min FPS = 60</code>). WebAP will use this value not as a hard limit, but as an anchor for rendering quality decisions.</li></ol>',12)])])}const u=t(r,[["render",n]]);export{p as __pageData,u as default};
